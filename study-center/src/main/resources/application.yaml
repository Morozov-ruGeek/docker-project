server:
  port: 8190

soap:
  payment-url: ${SOAP_URL} #http://localhost:8189/ws with eureka =

spring:
  application:
    name: study-center
#  profiles:
#    active: study-center
  config:
    import: optional:configserver:${CONFIG_URL:http://localhost:8888}

eureka:
  client:
    serviceUrl.defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}