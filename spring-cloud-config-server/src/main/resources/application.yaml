server:
  port: 8888
spring:
  application:
    name: spring-cloud-config
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Morozov-ruGeek/spring-cloud-config
#        bootstrap: true